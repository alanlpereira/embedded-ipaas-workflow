{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "turbo run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/((?!assets/|manifest.json|favicon.ico|sw.js).*)$",
      "destination": "/index.html"
    }
  ]
}
